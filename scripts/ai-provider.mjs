/**
 * AI 接口占位文件。
 *
 * 用户自行实现以下两个函数：
 *
 * - generateIntroZh(repoName, repoDescription, readmePreview) → string (50-100字)
 * - translateToEn(text) → string
 *
 * 环境变量：
 * - AI_API_KEY
 * - AI_API_BASE_URL  (可选)
 * - AI_MODEL         (可选)
 */

export async function generateIntroZh(repoName, repoDescription, readmePreview) {
  // TODO: 调用 AI API 生成 50-100 字中文简介
  return "";
}

export async function translateToEn(text) {
  // TODO: 调用 AI API 翻译中文为英文
  return "";
}
